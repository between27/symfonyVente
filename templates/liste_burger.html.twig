{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}





{% block content %}
    <a href="{{ path('home') }}" class="text-xl animate-bounce hover:bg-black">🏠</a>
    <div class="">

{% for burger in burgers %}
    <div>
        <a class="text-3xl font-bold underline m-0"
            href="{{ path('burger',{'id':burger.id}) }}">
            {{burger.name}}
        </a>
    </div>

    {% endfor %}
    </div>
{% endblock %}

{% block home %}
<a href="{{ path('home') }}">Accueil</a>
 {% endblock %} 